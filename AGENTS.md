# DeepSeek Harness Desktop

## 项目目标

将 https://github.com/deepseek-ai/deepseek-harness 封装为方便普通用户使用的桌面应用：安装后点击图标即可启动，在应用内完成必要配置，并能持续跟随上游版本更新。

默认先交付 Windows 版本。其他平台根据后续需求扩展。首次运行可以联网准备依赖，但应显示进度、失败原因和重试入口；日常启动不要求用户打开终端、安装开发工具或手动运行命令。

## 架构边界

- 优先复用上游核心、插件体系和 Web UI。桌面层负责窗口、运行环境、进程生命周期、本地配置入口和版本管理。
- 上游 README 当前提供 `npx @deepseek-ai/dsh web` 及 `--no-open` 启动方式。接入前核对选定版本的实际 CLI、运行时要求和就绪检测方式，不把示例命令当作已完成的打包方案。
- 将上游适配集中在独立模块；界面组件不直接依赖上游内部实现。确需修改上游时，保持补丁小、可追踪，并记录原因和适用版本。
- 桌面框架根据运行时分发、Windows 打包和上游接入验证结果选择，避免为尚未确定的功能引入复杂框架。
- 运行文件与用户数据分离。升级和卸载流程明确处理配置、会话与插件；升级不得覆盖或删除用户数据。
- 密钥使用系统凭据存储或合适的受保护机制，禁止写入仓库、日志和前端构建产物。桌面桥接只暴露必要能力，本地服务默认仅监听回环地址。

## 上游同步与应用更新

- 桌面版本与 Harness 版本分别记录并展示。依赖锁定到明确版本或提交，构建结果可追溯；日常启动不拉取浮动最新版。
- 区分“发现上游新版本”“验证兼容性”“提供用户更新”。跟随上游不代表每个新提交都立即自动安装。
- 为上游版本变化提供兼容性验证入口，至少覆盖启动、配置读取、基本交互及退出。根据实际接口变化调整适配模块。
- 更新先下载到独立位置，校验来源和完整性，通过验证后切换。失败时继续使用旧版本，并提供可理解的错误提示。
- 更新不得中断正在执行的任务。涉及数据迁移时先备份，并同时考虑旧运行时与数据格式的回退兼容性。
- 保留上游许可证和适用的第三方声明，清楚说明本项目是桌面封装项目。

## 首版完成标准

1. 提供实际可运行的 Windows 分发包；在没有开发环境的机器上验证首次启动与再次启动。
2. 点击图标后自动管理 Harness 服务并打开可用窗口，呈现准备中、就绪和失败状态。
3. 用户能够完成上游所需配置并执行一次真实的基本交互；缺少凭据时如实标记这项验收未完成。
4. 重复启动、端口占用、服务崩溃和退出有明确行为；只清理本应用拥有的进程。
5. 提供版本展示、检查更新、安装更新及失败回退流程；验证升级后用户数据仍可用。

这些是产品交付标准，不要求每个小改动都重跑全部验收。界面原型、模拟响应和开发服务器启动不能代替分发包验证。

## 协作方式

- 根据用户当前任务持续推进到对应的完成标准。常规实现选择、可逆编辑和相关本地验证直接执行；只在缺少会改变结果的必要信息时提出简短问题，并继续不依赖答案的工作。
- 按需读取相关代码和文档。本文只保留稳定的项目约束；架构决策、打包步骤和故障说明在需要时放入专题文档，不要求每次修改通读全仓库。
- 技能按实际任务使用，避免仅因关键词加载无关指导。用户明确要求优先于技能建议，同时遵守更高优先级指令；若技能导致停工，说明具体文件和条款。
- 验证与变更风险匹配。重点检查启动、进程管理、凭据和更新等关键路径；通过后不无故重复或扩大测试。文档改动检查内容和链接即可。
- 重做旧版前核实实际源码路径，保留可恢复快照及未提交内容。清理只针对已明确的旧源码范围，保留 Git 历史、用户数据和凭据；已有明确授权无需重复询问。
- 使用简洁中文沟通，说明完成内容、验证结果和实际阻塞；未运行的测试、未发布的安装包、未验证的上游兼容性不得宣称完成。

## 依据与维护

- 上游事实来源：https://github.com/deepseek-ai/deepseek-harness
- 指令编写参考：https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra

本文采用精简常驻指令、按需提供上下文和明确完成边界的原则。具体产品约束来自本项目需求与工程决策，并非文章原文。接入方式、技术栈或交付命令确定后更新相关文档，删除过时规则。
